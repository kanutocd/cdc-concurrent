# frozen_string_literal: true

RSpec.describe Cdc::Fiber do
  it "has a version number" do
    expect(Cdc::Fiber::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
